---
description: Read-only view of council state — branches, existing problems and runs, what's incomplete (drafts, incomplete runs), and the suggested next command. Modifies nothing.
argument-hint: [<branch>] [<problem-id>]
---

Activate the `council` skill and execute the **status** action with arguments: $ARGUMENTS.
