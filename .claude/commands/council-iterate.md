---
description: Conversational capture of a problem (council). Creates problem.md with [GAP] markers, fills them via chat, persists each response. Status draft → open on close. Active locale detected on first turn or via --lang=.
argument-hint: <branch> [<problem-id>] [--lang=es|en]
---

Activate the `council` skill and execute the **iterate** action with arguments: $ARGUMENTS.
