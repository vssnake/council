---
description: Refines an already-closed council deliberation without redoing it. Captures a follow-up, classifies it into a tier (1 clarification / 2 refinement / 3 out of scope), and produces a scoped child run reusing the parent run's panel and verified work. Adds a navigation appendix to the parent's outcome.md.
argument-hint: <branch> <problem-id> [<parent-run-id>] [<slug>]
---

Activate the `council` skill and execute the **refine** action with arguments: $ARGUMENTS.
