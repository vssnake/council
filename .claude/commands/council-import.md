---
description: Imports an external draft and normalizes it to the problem.md schema (council). After importing, offers to iterate to fill gaps. Active locale inferred from the source file or via --lang=.
argument-hint: <branch> --from-file=<path> [--lang=es|en]
---

Activate the `council` skill and execute the **import** action with arguments: $ARGUMENTS.
