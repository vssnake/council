---
description: Launches a council deliberation on a closed problem.md. Iterates hypothesis (skippable) + deliverable (mandatory) + 4-6 expert panel + 4 rounds (spawn primitive per expert) + synthesis by impartial moderator. The same <problem-id> can have N independent deliberations — each with its own slug.
argument-hint: <branch> <problem-id> [<slug>]
---

Activate the `council` skill and execute the **deliberate** action with arguments: $ARGUMENTS.
